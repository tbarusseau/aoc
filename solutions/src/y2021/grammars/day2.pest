direction = _{ forward | down | up }
    forward = { "forward" }
    down    = { "down" }
    up      = { "up" }

separator = _{ WHITE_SPACE }
newline = _{ NEWLINE }
value = { ASCII_DIGIT+ }

instruction = { direction ~ separator ~ value ~ newline }

instructions_set = { (instruction)+ }
